{
  "rules": {
    // Relawan details.
    "r": {
      "$uid": {
        ".read": "auth !== null",
        ".write": "$uid === auth.uid"
      }
    },

    // Hierarchy.
    "h": {
      "$id": { ".read": true }
    }

  }
}
